<h1>Netflix-Titel bewerten</h1>

<div class="card">
  <h2>Stranger Things</h2>
  <img src="https://via.placeholder.com/300x150?text=Stranger+Things" alt="Stranger Things">
  <div class="stars" data-title="Stranger Things">
    ★★★★★
  </div>
</div>

<div class="card">
  <h2>Dark</h2>
  <img src="https://via.placeholder.com/300x150?text=Dark" alt="Dark">
  <div class="stars" data-title="Dark">
    ★★★★★
  </div>
</div>
<div class="card">
  <h2>Dein Filmtitel</h2>
  <img src="https://via.placeholder.com/300x150?text=Dein+Film" alt="Dein Filmtitel">
  <div class="stars" data-title="Dein Filmtitel">
    ★★★★★
  </div>
</div>
<h1 style="color: #ff3c00;">Meine Netflix-Favoriten</h1>

<div class="card">
  <h2>Breaking Bad</h2>
  <img src="https://via.placeholder.com/300x150?text=Breaking+Bad" alt="Breaking Bad">
  <div class="stars" data-title="Breaking Bad">
    ★★★★★
  </div>
</div>

<div class="card">
  <h2>Haus des Geldes</h2>
  <img src="https://via.placeholder.com/300x150?text=Haus+des+Geldes" alt="Haus des Geldes">
  <div class="stars" data-title="Haus des Geldes">
    ★★★★★
  </div>
</div>

<div class="card">
  <h2>Wednesday</h2>
  <img src="https://via.placeholder.com/300x150?text=Wednesday" alt="Wednesday">
  <div class="stars" data-title="Wednesday">
    ★★★★★
  </div>
</div>
<div class="card">
  <h2>MEIN TITELNAME</h2>
  <img src="BILD-URL" alt="MEIN TITELNAME">
  <div class="stars" data-title="MEIN TITELNAME">
    ★★★★★
  </div>
</div>
<h2>Neuen Titel hinzufügen</h2>
<input type="text" id="titelName" placeholder="Serien-/Filmtitel">
<input type="text" id="bildUrl" placeholder="Bild-URL">
<button onclick="neuenTitelHinzufügen()">Hinzufügen</button>

<div id="inhalt"></div>

<script>
function neuenTitelHinzufügen() {
  const name = document.getElementById('titelName').value;
  const bild = document.getElementById('bildUrl').value;
  
  if (!name || !bild) {
    alert("Bitte gib Titel und Bild-URL ein.");
    return;
  }

  const inhalt = document.getElementById('inhalt');

  const card = document.createElement('div');
  card.className = 'card';
  card.innerHTML = `
    <h2>${name}</h2>
    <img src="${bild}" alt="${name}">
    <div class="stars" data-title="${name}">★★★★★</div>
  `;
  inhalt.appendChild(card);
  
  setupStars(); // Sterne aktivieren
  document.getElementById('titelName').value = '';
  document.getElementById('bildUrl').value = '';
}

// Sterne-Funktion
function setupStars() {
  document.querySelectorAll('.stars').forEach(stars => {
    const title = stars.getAttribute('data-title');
    let rating = 0;
    stars.innerHTML = '';
    for (let i = 1; i <= 5; i++) {
      const star = document.createElement('span');
      star.textContent = '★';
      star.classList.add('star');
      star.dataset.value = i;
      star.addEventListener('click', () => {
        rating = i;
        updateStars(stars, rating);
        alert(`Du hast "${title}" mit ${rating} Sternen bewertet.`);
      });
      stars.appendChild(star);
    }
  });
}

function updateStars(container, rating) {
  container.querySelectorAll('.star').forEach(star => {
    const val = parseInt(star.dataset.value);
    star.classList.toggle('selected', val <= rating);
  });
}

// Seite vorbereiten
setupStars();
</script>
<h2>Neuen Titel hinzufügen</h2>
<input type="text" id="titelName" placeholder="Serien-/Filmtitel">
<input type="text" id="bildUrl" placeholder="Bild-URL">
<button onclick="neuenTitelHinzufügen()">Hinzufügen</button>

<div id="inhalt"></div>
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <title>Netflix Bewertungen</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #ffffff;
      color: #b30000;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
    }
    input, button {
      font-size: 16px;
      padding: 10px;
      margin: 5px;
      border: 2px solid #b30000;
      border-radius: 5px;
      color: #b30000;
      background: white;
    }
    button {
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #b30000;
      color: white;
    }
    .card {
      background-color: #fff0f0;
      border: 2px solid #b30000;
      border-radius: 8px;
      padding: 20px;
      max-width: 400px;
      width: 90%;
      margin: 15px auto;
      text-align: center;
      box-shadow: 0 0 8px rgba(179, 0, 0, 0.2);
    }
    img {
      max-width: 100%;
      border-radius: 4px;
      border: 2px solid #b30000;
      margin: 10px 0;
    }
    .star {
      font-size: 26px;
      cursor: pointer;
      color: #999;
      user-select: none;
      margin: 0 3px;
      transition: color 0.3s ease;
    }
    .star.selected {
      color: #ff0000;
    }

    /* Mobile Anpassungen */
    @media (max-width: 600px) {
      body {
        padding: 10px;
      }
      input, button {
        width: 100%;
        box-sizing: border-box;
        margin: 8px 0;
      }
      .card {
        width: 100%;
        max-width: none;
        padding: 15px;
        margin: 10px 0;
      }
    }
  </style>
</head>
<body>
  <h1>Netflix Titel bewerten</h1>

  <input type="text" id="titelName" placeholder="Titel eingeben" />
  <input type="text" id="bildUrl" placeholder="Bild-URL eingeben" />
  <button onclick="neuenTitelHinzufügen()">Hinzufügen</button>

  <div id="inhalt"></div>

  <script>
    // Beim Laden gespeicherte Titel laden und anzeigen
    window.onload = function () {
      const gespeicherteTitel = JSON.parse(localStorage.getItem("titelListe")) || [];
      gespeicherteTitel.forEach(titel => addCard(titel.name, titel.bildUrl, titel.bewertung));
    };

    function neuenTitelHinzufügen() {
      const name = document.getElementById('titelName').value.trim();
      const bild = document.getElementById('bildUrl').value.trim();
      if (!name || !bild) {
        alert("Bitte Titel und Bild-URL eingeben.");
        return;
      }

      const neuerTitel = { name: name, bildUrl: bild, bewertung: 0 };
      const gespeicherteTitel = JSON.parse(localStorage.getItem("titelListe")) || [];
      gespeicherteTitel.push(neuerTitel);
      localStorage.setItem("titelListe", JSON.stringify(gespeicherteTitel));

      addCard(name, bild, 0);

      document.getElementById('titelName').value = '';
      document.getElementById('bildUrl').value = '';
    }

    function addCard(name, bildUrl, bewertung) {
      const inhalt = document.getElementById('inhalt');

      const card = document.createElement('div');
      card.className = 'card';
      card.innerHTML = `
        <h2>${name}</h2>
        <img src="${bildUrl}" alt="${name}" />
        <div class="stars" data-title="${name}"></div>
      `;
      inhalt.appendChild(card);
      setupStars(card.querySelector('.stars'), name, bewertung);
    }

    function setupStars(container, title, aktuelleBewertung) {
      container.innerHTML = '';
      for (let i = 1; i <= 5; i++) {
        const star = document.createElement('span');
        star.textContent = '★';
        star.classList.add('star');
        star.dataset.value = i;
        if (i <= aktuelleBewertung) star.classList.add('selected');
        star.addEventListener('click', () => {
          updateStars(container, i);
          speichernBewertung(title, i);
        });
        container.appendChild(star);
      }
    }

    function updateStars(container, rating) {
      container.querySelectorAll('.star').forEach(star => {
        star.classList.toggle('selected', parseInt(star.dataset.value) <= rating);
      });
    }

    function speichernBewertung(titelname, neueBewertung) {
      let gespeicherteTitel = JSON.parse(localStorage.getItem("titelListe")) || [];
      gespeicherteTitel = gespeicherteTitel.map(titel => {
        if (titel.name === titelname) {
          titel.bewertung = neueBewertung;
        }
        return titel;
      });
      localStorage.setItem("titelListe", JSON.stringify(gespeicherteTitel));
    }
  </script>
</body>
</html>
